I'm having issues with an Azure Functions v4 (Node.js) deployment. The function works locally but returns a 404 in Azure. Here are the details:

1. Current Project Structure:
src/
├── index.ts (imports './fetch-items')
└── fetch-items/
    └── index.ts (contains the function implementation)

2. Function Implementation (fetch-items/index.ts):
```typescript
import { app, HttpRequest, HttpResponseInit, InvocationContext } from "@azure/functions";
import { CosmosClient } from "@azure/cosmos";

const DB_ID = "avs-db";
const CONTAINER_ID = "avs-av";

export async function fetchItems(req: HttpRequest, ctx: InvocationContext): Promise<HttpResponseInit> {
  ctx.log("Function fetch-items triggered.");

  const conn = process.env.COSMOS_DB_CONNECTION;
  if (!conn) {
    ctx.log("COSMOS_DB_CONNECTION missing");
    return { status: 500, body: "DB connection not configured" };
  }

  const client = new CosmosClient(conn);
  const { resources } = await client.database(DB_ID).container(CONTAINER_ID).items.readAll().fetchAll();

  return { status: 200, jsonBody: resources };
}

// Register the function
app.http("fetch-items", {
  methods: ["GET"],
  authLevel: "anonymous",
  handler: fetchItems,
  route: "fetch-items"
});
```

3. Main index.ts:
```typescript
import './fetch-items';
```

4. Current Deployment Structure (from GitHub Actions):
dist/
├── host.json
├── index.js
└── fetch-items/
    └── index.js

5. Key Points:
- Using Azure Functions v4
- Node.js runtime
- Programming model: @azure/functions
- Function works locally (http://localhost:7071/api/fetch-items)
- Getting 404 on https://avs-api.azurewebsites.net/api/fetch-items
- Function is not showing up in Azure Portal
- No function.json files (using v4 programming model)
- Using the new programming model with app.http() registration

6. What we've tried:
- Removed all function.json files
- Updated to v4 programming model
- Simplified project structure
- Ensured proper exports and imports
- Verified local functionality
- Checked GitHub Actions deployment logs

The function works perfectly locally but is not being recognized in Azure. What could be causing this issue with the v4 programming model deployment? 