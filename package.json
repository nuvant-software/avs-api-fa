{
  "name": "avs-api-fa",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc && copyfiles host.json dist",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "rimraf dist && tsc && copyfiles host.json dist",
    "start": "npm run start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.4.1",
    "@azure/identity": "^4.10.0"
  },
  "devDependencies": {
    "@azure/functions": "^4.7.2",
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "copyfiles": "^2.4.1",
    "cpx": "^1.2.1",
    "rimraf": "^5.0.10",
    "typescript": "^4.0.0"
  }
}
